

{% for collaborateur in collaborateurs %}
    <tr>
        <td>{{collaborateur.nom}}</td>
        <td>{{collaborateur.prenom}}</td>
        <td>{{collaborateur.mail}}</td>
        <td>{{collaborateur.dateEmbauche | date('d/m/Y')}}</td>
        <td><a href="{{path('detail_collaborateur', {'id' : collaborateur.id})}}" title="Détail du collaborateur">svg profil</a></td>
    </tr>
{% endfor %}